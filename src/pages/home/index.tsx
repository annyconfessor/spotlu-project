import { Box } from "@/components"
import { ContainerLayout } from "@/components/Layout"

const Home = () => {
  return (
  <ContainerLayout>
    <Box height="100vh" />
  </ContainerLayout>
  )
}

export default Home